#version 330 core

// Valores interpolados dos sombreadores de vértice
in vec3 fragmentColor;

// Ouput data
out vec3 color;

void main(){

	// Cor de saída = cor especificada no sombreador de vértice,  
	// interpolada entre todos os 3 vértices circundantes
	color = fragmentColor;

}